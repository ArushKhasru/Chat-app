import React from 'react'
import { useChatStore } from '../store/useChatStore';

export default function ContactList() {
  const { allContacts, isUsersLoading } = useChatStore();
  return (
    <div>
      
    </div>
  )
}
